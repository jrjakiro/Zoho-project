//For pushing elements into the stack since it is dynamic can grow while pushing

void StackNode :: push(StackNode** root, int data)
{
	StackNode* stackNode = newNode(data);
	stackNode->next = *root;
	*root = stackNode;
	cout << data << " pushed to stack\n";
}
